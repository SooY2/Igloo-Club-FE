import styled from '@emotion/styled';

const LimitModal = () => {
  return <StModalContainer></StModalContainer>;
};

export default LimitModal;

const StModalContainer = styled.div`
  display: flex;
  flex-direction: column;
`;
